{
  "name": "datapresent-extension",
  "version": "1.0.0",
  "description": "DataPresent Browser Extension",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "typescript": "^5.7.0"
  }
}
